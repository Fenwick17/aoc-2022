import * as fs from 'fs';

const input: string = fs.readFileSync('day-05/input.txt').toString();
const [crates, instructions] = input.split('\n\n');

function moveCrates(crates: [], from: number, to: number) {}
